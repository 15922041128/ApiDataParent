<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.pbccrc.api.core.mapper.PersonLogMapper">
	
	<insert id="addLog" parameterType="org.pbccrc.api.base.bean.PersonLog">
		insert into personLog(id, ipAddress, userID, operatorType, totalCount, successCount, failCount, queryDate, note) 
			values(seq_personlog, #{ipAddress}, #{userID}, #{operatorType}, #{totalCount}, #{successCount}, #{failCount}, #{queryDate}, #{note})
	</insert>
	
</mapper>
