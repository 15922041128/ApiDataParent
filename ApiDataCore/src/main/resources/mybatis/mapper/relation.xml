<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.pbccrc.api.core.mapper.RelationMapper">
	
	<select id="queryAll" resultType="org.pbccrc.api.base.bean.Relation">
		select * from relation
	</select>
	
	<insert id="addRelation" parameterType="org.pbccrc.api.base.bean.Relation" >
		<selectKey resultType="java.lang.Integer" order="BEFORE" keyProperty="id">  
	        SELECT seq_relation.nextval as id from dual
	    </selectKey>  
		insert into relation(id, userID, apiKey, productID, count, costType, price, createDate, allowIps, visitCount)
			values(#{id}, #{userID, jdbcType=INTEGER}, #{apiKey, jdbcType=VARCHAR}, #{productID, jdbcType=INTEGER}, #{count, jdbcType=INTEGER}, 
			#{costType, jdbcType=INTEGER}, #{price, jdbcType=FLOAT}, #{createDate, jdbcType=VARCHAR}, #{allowIps, jdbcType=VARCHAR}, #{visitCount, jdbcType=INTEGER})
	</insert>
	
	<update id="resetVisitCount">
		update relation set visitCount = 1000
	</update>
	
</mapper>