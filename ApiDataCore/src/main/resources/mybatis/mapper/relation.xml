<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.pbccrc.api.core.mapper.RelationMapper">
	
	<select id="queryAll" resultType="java.util.HashMap">
		select * from relation
	</select>
	
	<insert id="addRelation" parameterType="org.pbccrc.api.base.bean.Relation" keyProperty="ID">
		insert into relation(userID, apiKey, productID, count, costType, price, createDate, allowIps)
			values(#{userID}, #{apiKey}, #{productID}, #{count}, #{costType}, #{price}, #{createDate}, #{allowIps})
	</insert>
	
</mapper>