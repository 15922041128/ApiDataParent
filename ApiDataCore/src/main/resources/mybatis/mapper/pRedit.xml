<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.pbccrc.api.core.mapper.PReditMapper">
	
	<insert id="addPRedit" parameterType="java.util.HashMap">
		insert into p_redit(
			personID, contactDate, hireDate, expireDate, type, loanUsed, 
			totalAmount, balance, status, bizOccurOrg, createUser, createTime, updateUser, updateTime) 
		values(
			#{personID}, #{contactDate}, #{hireDate}, #{expireDate}, #{type}, #{loanUsed}, 
			#{totalAmount}, #{balance}, #{status}, #{bizOccurOrg}, #{createUser}, #{createTime}, #{updateUser}, #{updateTime})
	</insert>
	
	<select id="queryAll" parameterType="java.lang.String" resultType="java.util.HashMap">
		select * from p_redit where personID = #{personID} 
	</select>
	
</mapper>